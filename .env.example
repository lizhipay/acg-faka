# 复制成 .env 再改：cp .env.example .env
#
# 数据库密码没有默认值 —— 发卡系统装完就会挂到公网，里面是订单、卡密和余额，
# 用示例密码等于把数据库直接送出去。请务必改成随机值，例如：
#   openssl rand -base64 24

MYSQL_ROOT_PASSWORD=请改成随机密码
MYSQL_PASSWORD=请改成随机密码

# 下面这些有默认值，按需改
MYSQL_DATABASE=acg_faka
MYSQL_USER=acg

# 站点对外端口
ACG_HTTP_PORT=8080
